apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: shopfast
spec:
  selector:
    app: shopfast-db
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  clusterIP: None  # Headless service for StatefulSet
  type: ClusterIP
